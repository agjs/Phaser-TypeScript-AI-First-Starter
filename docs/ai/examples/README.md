# Examples

Side-by-side good/bad code to pattern-match against. Not compiled (see
`eslint-disable`/`@ts-nocheck` at the top of each).

| Concern | Good | Bad |
|---|---|---|
| Domain behavior | `good-behavior.ts` | `bad-behavior.ts` |
